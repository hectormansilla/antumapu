{% extends "base_template.html.j2" %}

{% block title %}IBM BECh: Tiempo Excedido{% endblock %}

{% block content %}
  <h1 class="display text-center">Reporte de procesos sin información</h1>
    <div class="row">
      <table class="table">
        <table class="table">
          <thead>
            <tr>
              <th scope="col">Proceso</th>
              <th scope="col">Malla</th>
              <th scope="col">Inicio</th>
            </tr>
          </thead>
            {% for task in tasks %}
          <tbody>
            <tr>
              <th scope="row">{{task.proceso}}</th>
              <td>{{task.malla}}</td>
              <td>{{task.inicio}}</td>
            </tr>
            {% endfor %}
          </tbody>
      </table>
    </div>
{% endblock %}
